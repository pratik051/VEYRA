import { PolicyTemplate } from "@/components/ui/policy-template";

export default function ReturnPolicyPage() {
  return <PolicyTemplate title="Return Policy" body="Return eligibility is determined by product type, condition, and supplier terms. Customers should review item-specific return rules before confirmation." />;
}

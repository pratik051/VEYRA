import { PolicyTemplate } from "@/components/ui/policy-template";

export default function CancellationPolicyPage() {
  return <PolicyTemplate title="Cancellation Policy" body="Cancellations may be possible before sourcing/dispatch stages. Eligibility is determined by current order stage and supplier commitments." />;
}
